services:
  - type: web
    name: feature-flag-engine
    runtime: python
    plan: free
    buildCommand: cd server && python seed_data.py
    startCommand: cd server && python -m uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
